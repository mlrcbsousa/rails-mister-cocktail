<!-- views/cocktails/_dose.html.erb -->
<%= link_to dose_path(dose),
      method: :delete,
      data: { confirm: "Are you sure?" } do %>
        <i class="far fa-trash-alt"></i>
      <% end %>
<strong><%= dose.description %>:</strong>
<%= dose.ingredient.name %>
